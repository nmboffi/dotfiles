""" GENERAL SETTINGS
syntax enable
set nocompatible        
set backspace=2
set tabstop=4 softtabstop=4 expandtab shiftwidth=4 smarttab
set mouse=a

" Improves loading of large files
set lazyredraw
set ttyfast

""" vim-plug
" set the runtime path to include Vundle and initialize
call plug#begin('~/.vim/plugged')

" My plugins
Plug 'flazz/vim-colorschemes'                     " Colors
Plug 'Valloric/YouCompleteMe', { 'do': './install.py --clang-completer' }
Plug 'scrooloose/nerdtree'                        " File browser
Plug 'godlygeek/tabular'                          " Better automatic indenting
Plug 'Raimondi/delimitMate'                       " Smart brace autoclose
Plug 'scrooloose/nerdcommenter'                   " Quick and easy comment hotkeys
Plug 'chriskempson/base16-vim'                    " Base16 colorschemes
Plug 'sjl/gundo.vim'                              " Access vim's undo tree
Plug 'Konfekt/fastfold'                           " Speed up folding
"Plug 'bbchung/clighter8'                              " Better C-family syntax highlighting
Plug 'octol/vim-cpp-enhanced-highlight'
Plug 'mhinz/vim-startify'
Plug 'tpope/vim-sensible'
Plug 'justinmk/vim-syntax-extra'

" All of your Plugins must be added before the following line
call plug#end()              " required
filetype plugin indent on    " required

" clamp
"let g:clighter8_autostart=1
"let g:clighter8_libclang_file = '/usr/lib/llvm-3.8/lib/libclang.so'

" cpp highlighting
let g:cpp_class_scope_highlight = 1
let g:cpp_experimental_template_highlight = 1
let g:cpp_concepts_highlight = 1

"YCM options
let g:ycm_global_ycm_extra_conf = '~/.config/.ycm_extra_conf.py'

"Colorscheme
let base16colorspace=256
colorscheme base16-paraiso
set background=dark
set number

" Allow for transparency with i3 and compton
highlight Normal ctermbg=none
highlight NonText ctermbg=none
